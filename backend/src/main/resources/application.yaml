spring:
  application:
    name: OrderManager

  profiles:
    active: ${SPRING_PROFILES:test}

  jpa:
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  server:
    port: 8080
    address: 0.0.0.0